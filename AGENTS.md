# AGENTS.md —— 在这个仓库里干活的规矩

> 这份文件只写**怎么做**；"为什么"一律在指针里，不在这里复制。
> 与 `docs/` 冲突时以 `docs/` 为准。

## 0. 先读什么（顺序别乱）

1. **`docs/PLAN.md` —— 总计划，所有阶段的来源**：G1–G7 成功判据、S0–S10 阶段划分、已验证事实与风险。
   改它的**正文 = 改整个项目的承诺**（S5 就修正过它的一个事实错误）。
2. **`docs/STATUS.md` —— 唯一入口**：现在在哪 / 欠着什么 / 下一步。**每次提交前**更新"进行中"那一行。
3. **当前阶段的 `docs/S<n>-plan.md`**：§3 事实 / §4 决策 / §6 断言 / §7-§8 验收 / §9 步骤 /
   §10 评审记录（为什么这么定）/ §11 实施期发现（踩过的坑）。
4. 收尾看 **README 的已知限制**（给用户的最终事实）与各 plan 的 §12（实测结果）。

> ✅ **总计划只有一份：`docs/PLAN.md`**。2026-09-13 已把根目录那份（gitignore、带 16 轮评审记录）
> 并进来并删除 —— 所以不用再担心两份分叉。那 16 轮评审记录就在本文末的 `## 评审记录` 里。

## 1. 人工动作预算（最容易被违反的一条）

- 默认是"**我自己做**"，不是"让用户测一下"。
- 每留一条人工项，必须写清它属于哪一类 ——
  **①排版/外观 ②真焦点/真键盘 ③真进程 ④Windows 路径与 shell 形态** —— **以及为什么自动化不了**。
  不属于这四类的一律自动化。
- 能合并就合并、能搬到我这边就搬（换做法 / 真 spawn 命令 / 换机器）。
  **软上限：Mac 2 个、Windows 2 个**；超过要逐条辩论（判例：S5-plan §10.1 的 U5）。
- 用户只有两件事不该省：**点一下命令把结果贴回来**、**Marketplace 手动上传**（没有 PAT）。
- 验收挂了就**再来一轮**（修 → 版本 +1 → 重验），别为了少一轮而放宽验收。

## 2. 断言

- **每步先写断言、看红、再实现**。**红必须是断言失败** —— 编译不过、模块不存在都不算红。
- 断言要**独立于被测实现**：隔离目录 import、问另一份实现（S5 的 T12 直接去问终端那份 pi）、
  必要时**故意复刻一份规则**当 oracle（`controller-check` 的编码规则就是这么写的）。
- 新断言要有**能红验证**（故意改坏看一眼红）；**不许对着想象中的实现写断言**（会退化成实现的镜像）。
- **SKIP 不是 PASS**；而且 SKIP 必须能区分"真的没有"与"我找错了"
  （S5-plan §11 的 1-4：PATH 拆错分隔符、`pi` 是符号链接 —— 两次都表现为静默 SKIP）。
- `host-check` 的桩纪律：真输入、桩只在断言端、用真类与真 HTML。

## 3. 事实从哪来

- 读语义看 `node_modules/@earendil-works/pi-coding-agent/dist/...`（引用要带 `文件:行号`）；
  判断"能不能用"看 `pi-runtime/dist/bundle` 的**导出面**。**没导出 ≠ 不可能**
  （可以复刻规则 + 加守卫，或去问另一份安装）。
- **实测优先**；引用实测数字；没实测的**明说"未实测"**。
- 上游是**钉死的版本，不是稳定契约** → 依赖它就要配一条漂移守卫。
- 最险的三条 pi 语义：① `sessionDir` 是"直接装 `.jsonl` 的目录"，**不是** sessions 根；
  ② `isPersisted()` 不是"落盘了没"；③ `teardownCurrent()` 会先 `abort()`、并把该轮落盘。
  其余见 **`docs/pi-traps.md`**（索引，改与 pi 交互的代码前扫一遍）。

## 4. 别动用户的东西

- 不替用户**移动/改写**数据（`~/.pi/agent` 下的会话、工作区文件）；给恢复/迁移办法，别自己动手。
- **别在用户真实的 `agentDir` 里留垃圾**：会 `mkdirSync` 的 API 用真实 cwd，或建了就清理
  （清理要带"必须在我的根之下"的守卫）。
- **不自己拼会话 JSONL** —— 一律经 `SessionManager`（这是"格式一定对"的唯一保证）。

## 5. 改代码

- **改名强迫编译器抓误用**（例：`sessionsDir` → `sessionsRoot`），别指望注释。
- 注释写"为什么"和"踩过的坑"，不写"做了什么"。
- **每次 edit 之后看 `git diff`** —— 编辑工具的模糊匹配会咬人（S5-plan §11 的 1-8）。
- 一步一提交；每步结束时仓库必须可用、门禁全绿；不夹带无关改动。

## 6. 评审（第二意见）

- **≤3 轮**；第 3 轮只核转写、不审设计；每轮结论**立刻落盘**（断点恢复锚点）。
- 每条明确 `ACCEPT` / `REJECT` / `DEFER`；**REJECT 要有实质依据**。
- **评审的意见也要自己核**：S5 第 2 轮那句"STATUS 的 9/9 是错的"其实是对的，用实跑驳回了。
- 三轮之后写进计划的改动，**必须标"未经复核"**。

## 7. 交付与发布

- 门禁全绿才交；**真模型那几条要真跑**，不许拿 SKIP 交差。
- **打包后 `unzip -l` 看一遍**：别把开发文件打进 VSIX（`.pi/settings.json` 就被打进去过 → `AGENTS.md` 也排除）。
  文件数变了先查清多的是哪个再谈是否回归。
- 顺序：**打包 → 自测 → Mac 验收 → 上传 → Windows 验收**（别把本地就挂的包发出去）。
- 报告给用户时三样都要有：**改了什么 / 证据 / 不确定的部分**。
- **承诺的检查必须落地**：计划里写了"改由我自动跑"的项，交付前 `grep` 一遍确认它真的在。
  S5 就漏过一次 —— 把"CLI 互通"从用户的人工动作里删掉、自动版却没写，于是那件事**没人验**。
- 语言：中文；提交信息**英文主题 + 中文正文（带证据与理由）**。
